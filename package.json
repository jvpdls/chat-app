{
    "name": "chat-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "npm install && cd front-end && npm install && cd ../api && npm install",
        "build": "cd front-end && npm run build",
        "dev": "concurrently \"cd front-end && npm run dev\" \"cd api && npm start\"",
        "start": "concurrently \"cd front-end && npm start\" \"cd api && npm start\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}